---

# TODO: Parametrize the job.

- name: Start workers
  become: yes

  hosts:
    - tag_osp_cluster-worker

  roles:
    - role: start-job
      job: 'worker:'

- name: Start ventilator
  become: yes

  hosts:
    - tag_osp_cluster-server

  roles:
    - role: start-job
      job: ventilator
