---

dependencies:

  - role: supervisor

    supervisor_programs:

      - name: producer
        command: >
          {{ osp_src_dir }}/env/bin/python
          {{ osp_src_dir }}/bin/producer.py

      - name: consumer

        command: >
          {{ osp_src_dir }}/env/bin/python
          {{ osp_src_dir }}/bin/consumer.py

        numprocs: 4

        config:
          autostart: True
